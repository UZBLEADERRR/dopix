<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overlayRoot"
    android:layout_width="72dp"
    android:layout_height="72dp">

    <!-- Pulse ring (shown when active) -->
    <View
        android:id="@+id/pulseRing"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_overlay_active"
        android:alpha="0"
        android:visibility="visible" />

    <!-- Main button -->
    <ImageView
        android:id="@+id/overlayIcon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_dopix"
        android:background="@drawable/bg_overlay_idle"
        android:padding="10dp"
        android:alpha="0.35"
        android:contentDescription="Dopix overlay button"
        android:scaleType="fitCenter" />

</FrameLayout>
